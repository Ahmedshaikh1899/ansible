---
- name: Install Apache
  yum:
    name: "{{ apache_package }}"
    state: present
- name: Start and enable Apache service
  service:
    name: "{{ apache_service }}"
    state: started
    enabled: yes
